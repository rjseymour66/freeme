console.log('This site was generated by Hugo.');

const sidebarDirs = document.querySelectorAll('.is-section');

const displaySubdir = e => {
    e.preventDefault();
    const child = e.target.querySelector('.sidebar-nav__item');
    console.log(child);
};

sidebarDirs.forEach(el => el.addEventListener('click', displaySubdir));